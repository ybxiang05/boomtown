import { createStyles } from "@material-ui/core";

const styles = theme => ({
  cardGrid: {
    display: "flex",
    flexWrap: "wrap",
    padding: "100px 3%"
  },
  singleCard: {
    maxWidth: "33%"
  }
});

export default styles;
