import { createStyles } from "@material-ui/core";

const styles = theme =>
  createStyles({
    cardGrid: {
      padding: "100px 3%"
    },
    singleCard: {
      width: "400px"
    }
  });

export default styles;
