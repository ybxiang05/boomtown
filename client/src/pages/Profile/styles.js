import { createStyles } from "@material-ui/styles";

const styles = theme => ({
  container: {
    backgroundColor: "black",
    height: "100%"
  },
  itemCard: {
    display: "flex",
    flexFlow: "row wrap"
  }
});

export default styles;
